---
page_style: content
background: "#dce8e6"
text_size: large
caption: A color background is a hex value or a CSS color name
notes: |
  The body is set large because it is one line.

  This theme sets dark type, so pick a ground it can be read against. A dark
  background needs a theme whose type is light.
---

# A Slide Can Set Its Own Ground

Give `background` a hex value or a color name and the slide takes it.
